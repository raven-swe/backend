meta {
  name: logout
  type: http
  seq: 14
}

post {
  url: http://localhost:3000/auth/logout
  body: none
  auth: bearer
}

headers {
  X-Client-Type: web
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
