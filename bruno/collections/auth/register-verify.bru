meta {
  name: register-verify
  type: http
  seq: 2
}

post {
  url: http://localhost:3000/auth/register/verify
  body: json
  auth: inherit
}

body:json {
  {
    "otp":"933503",
    "creationToken":"{{creationToken}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
