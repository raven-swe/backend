meta {
  name: mark-all-seen
  type: http
  seq: 2
}

patch {
  url: http://localhost:3000/notifications/seen
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
