meta {
  name: mark-all-as-seen
  type: http
  seq: 2
}

patch {
  url: http://localhost:3000/notifications/mark-all-seen
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
