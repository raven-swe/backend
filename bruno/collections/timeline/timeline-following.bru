meta {
  name: timeline-following
  type: http
  seq: 1
}

get {
  url: http://localhost:3000/tweets/timeline?cursor&limit=20
  body: none
  auth: bearer
}

params:query {
  cursor: 
  limit: 20
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
