meta {
  name: tweets
  type: http
  seq: 1
}

get {
  url: {{base_url}}/users/:username/tweets
  body: none
  auth: bearer
}

params:query {
  ~limit: 2
  ~cursor: 
}

params:path {
  username: gelgel
}

auth:bearer {
  token: {{access_token}}
}

vars:pre-request {
  username: Tasneem
  base_url: http://localhost:3000
}

settings {
  encodeUrl: true
  timeout: 0
}
