meta {
  name: mutes
  type: http
  seq: 16
}

get {
  url: {{base_url}}/me/settings/mutes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
