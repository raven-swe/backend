meta {
  name: change-language
  type: http
  seq: 11
}

put {
  url: http://localhost:3000/me/settings/language
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "language": "AR"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
