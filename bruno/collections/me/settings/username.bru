meta {
  name: username
  type: http
  seq: 4
}

patch {
  url: http://localhost:3000/me/settings/username
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
