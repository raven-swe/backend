meta {
  name: validate-password
  type: http
  seq: 12
}

post {
  url: http://localhost:3000/me/settings/password/validate
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "password": "Test$12345"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
